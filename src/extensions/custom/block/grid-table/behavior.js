import Behavior from "../../../behavior.js";

class GridTableBehavior extends Behavior {
  constructor(type) {
    super(type);
  }
  
  mount(root) {
    return;
  }
  
  unmount(root) {
    return;
  }
}

export { GridTableBehavior };