import Behavior from "../../../behavior.js";

class SoftBreakBehavior extends Behavior {
  constructor(type) {
    super(type);
  }
  
  mount(root) {
    return;
  }
  
  unmount(root) {
    return;
  }
}

export { SoftBreakBehavior };