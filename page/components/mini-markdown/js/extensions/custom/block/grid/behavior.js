import Behavior from "../../../behavior.js";

class GridBehavior extends Behavior {
  constructor(type) {
    super(type);
  }
  
  mount(root) {
    return;
  }
  
  unmount(root) {
    return;
  }
}

export { GridBehavior };