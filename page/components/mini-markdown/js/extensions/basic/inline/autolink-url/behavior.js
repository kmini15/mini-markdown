import Behavior from "../../../behavior.js";

class AutolinkUrlBehavior extends Behavior {
  constructor(type) {
    super(type);
  }
  
  mount(root) {
    return;
  }
  
  unmount(root) {
    return;
  }
}

export { AutolinkUrlBehavior };