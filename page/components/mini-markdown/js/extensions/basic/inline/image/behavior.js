import Behavior from "../../../behavior.js";

class ImageBehavior extends Behavior {
  constructor(type) {
    super(type);
  }
  
  mount(root) {
    return;
  }
  
  unmount(root) {
    return;
  }
}

export { ImageBehavior };