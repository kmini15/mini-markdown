import Behavior from "../../../behavior.js";

class AutolinkEmailBehavior extends Behavior {
  constructor(type) {
    super(type);
  }
  
  mount(root) {
    return;
  }
  
  unmount(root) {
    return;
  }
}

export { AutolinkEmailBehavior };