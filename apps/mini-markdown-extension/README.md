# Mini Markdown Extension for Visual Studio Code

This is a Visual Studio Code extension that provides a preview of the HTML output and the abstract syntax tree (AST) for Mini Markdown files. It allows users to visualize the structure of their Mini Markdown documents and see how they will be rendered as HTML.
