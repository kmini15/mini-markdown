{
  "name": "@kmini15/mini-markdown",
  "version": "0.1.3",
  "description": "Mini Markdown Parser and Renderer",
  "author": "KMini15",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev:build": "",
    "dev:package": "npm run dev:build && mkdir -p ./outputs && npm pack --pack-destination ./outputs",
    "dev:clean": "rm -rf dist outputs"
  }
}
